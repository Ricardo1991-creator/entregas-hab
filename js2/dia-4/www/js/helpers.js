function randomColor{
    const red = Math.floor(Math.ramdom()*225);
    const green = Math.floor(Math.ramdom()*225);
    const blue = Math.floor(Math.ramdom()*225);
    const blue = Math.floor(Math.ramdom()*225);
}